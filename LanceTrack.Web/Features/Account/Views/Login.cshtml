@model LanceTrack.Web.Features.Account.Models.LoginModel
@using Rx = LanceTrack.Web.Features.Account.UI

@section head {
    <link href="@Links.Features.Account.styles.login_min_css" type="text/css" rel="stylesheet" />
}

<div class="login-form panel panel-default">
    <form class="panel-body"
          method="POST" action="@Url.Action(MVC.Account.Login())">
        @Html.ValidationSummary(true)
        <div class="form-group">
            @Html.LabelFor(m => m.Login, Rx.Login)
            @Html.TextBoxFor(m => m.Login, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Login)
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Password, Rx.Password)
            @Html.PasswordFor(m => m.Password, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Password)
        </div>
        <div class="form-group">
            <div class="checkbox">
                <label>
                    @Html.CheckBoxFor(m => m.RememberMe) @Rx.RememberMe
                </label>
            </div>

        </div>
        <div class="form-group">
            <button class="btn btn-default btn-primary" type="submit">@Rx.Enter</button>
        </div>
    </form>
</div>